package com.bintina.goouttolunchmvvm.userwithrestaurant

import android.app.Application
import com.bintina.goouttolunchmvvm.restaurants.work.ResetWork
import kotlinx.coroutines.runBlocking
import org.junit.Assert.assertEquals
import org.junit.Test

class ResetWorkTest {
  /*  @Test
    fun testResetWork() = runBlocking {
        // Mock or spy methods
        val appContext = Application.CONTEXT_INCLUDE_CODE
        val worker = ResetWork(appContext, workerParams)

        // Simulate execution
        val result = worker.doWork()

        // Validate the result
        assertEquals(Result.success(), result)
        // Ensure mock methods were called
    }*/
}